{
  "API_SERVERS": [
    "http://localhost:7860",
    "https://your-other-server.com"
  ],
  // Optional: Enable detailed logging of job events to files in the 'logs' directory.
  // Defaults to true if omitted.
  "ENABLE_JOB_LOGGING": true 
}
